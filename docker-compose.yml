services:
  mysql:
    image: mysql:8
    container_name: mysql-cine
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: cine
    ports:
      - "3306:3306"

  app:
    build: .
    container_name: peliculas-app
    ports:
      - "3001:3000"
    depends_on:
      - mysql